import { Variant } from '../__data__/definitions';
import { colors } from './colors';

export const borderRadius = ['4px', '9999px'];

export const borderWidths = ['0.5px', '2px'];

export const borderColor = colors[Variant.Light].hex;
