export enum FontFamily {
  Default,
  Heading,
}

export enum FontSize {
  Large = 4,
  Default = 5,
  Small = 6,
}

export enum Heading {
  h1,
  h2,
  h3,
  h4,
  h5,
}

export enum Variant {
  Default,
  Light,
  Primary,
  Warning,
  Success,
}

export enum Resolution {
  Phone,
  Tablet,
  Desktop,
  HD,
}

export enum Spacing {
  XL,
  L,
  M,
  S,
  XS,
}
