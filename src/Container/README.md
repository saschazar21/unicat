Renders a default `<div />` element with a preset padding:

```jsx
import Heading from '../Heading';

<Container>
  <Heading level="h2">Look Ma!</Heading>
  <p>
    I am a paragraph inside a <code>&lt;Container&gt;</code>.
  </p>
</Container>;
```
